package model;

public class RegularBook extends Book {
    public RegularBook(String title, String author, int year, boolean borrowable, int baseBorrowDays) {
        super(title, author, year, borrowable, baseBorrowDays);
    }
}
